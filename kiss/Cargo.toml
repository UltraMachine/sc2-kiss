[package]
name = "sc2-kiss"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
sc2-core = { version = "0.1.0", path = "../core" }
num_enum = { version = "0.7", optional = true }

[features]
default = ["ids"]
ids = ["dep:num_enum"]
